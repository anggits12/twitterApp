import 'package:flutter/material.dart';

class DMPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Direct Messages Page", style: TextStyle(color: Colors.white, fontSize: 24)),
    );
  }
}
